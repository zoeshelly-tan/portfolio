import { useEffect, useState } from 'react';
export const useDelay = (delay = 0) => {
    const [done, setDone] = useState(false);
    useEffect(() => {
        setTimeout(() => {
            setDone(true);
        }, delay);
    }, [delay]);
    return done;
};
//# sourceMappingURL=useDelay.jsx.map