import { useMemo } from 'react';
export function useBindToInput(useInputResult) {
    const [values, actions] = useInputResult;
    return [
        values,
        actions,
        useMemo(() => ({
            eventBind: {
                onChange: actions.onChange,
                value: values[0],
            },
            valueBind: {
                onChange: actions.setValue,
                value: values[0],
            },
        }), [actions, values]),
    ];
}
export default useBindToInput;
//# sourceMappingURL=useBindToInput.js.map